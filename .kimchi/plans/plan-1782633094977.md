## New error: `MONGODB_URI` is being overridden to an invalid string at startup

The Spring Boot app is now crashing **before** serving any request, because the `MongoClient` bean can't even be built — Spring Boot's `PropertiesMongoConnectionDetails.getConnectionString` rejects the value it receives. The `server/.env` file itself is still correct:

```
MONGODB_URI=mongodb+srv://hettiarachchianuk01_db_user:lLqnesGwHLrQXkWj@cluster0.uc9vjll.mongodb.net/?appName=Cluster0
```

…so the bad value is coming from **outside the repo** — most likely one of:

- A stale `export MONGODB_URI=...` in your shell rc (`~/.bashrc`, `~/.profile`, `~/.zshrc`) set to an empty string, a placeholder, or an old URI that was edited.
- An `MONGODB_URI=...` prefix on the `mvn` invocation.
- A `-DMONGODB_URI=...` Maven system property.

Spring's property precedence is: command line / OS env > `application.properties` > `optional:` imports of `.env`. So whatever your shell is exporting wins over the `.env` file.

I can't read your shell environment from plan mode — that's the one missing piece.

## Plan to fix

The fix is a single shell + file change, then a restart. Pick the URI target and I'll do the rest as soon as you let me out of plan mode (or run the one diagnostic below).

### Option A — Local MongoDB via Docker (offline, no Atlas needed)
1. `docker run -d --name paf-mongo -p 27017:27017 -v paf-mongo-data:/data/db mongo:7`
2. Edit `server/.env` so `MONGODB_URI=mongodb://127.0.0.1:27017/paf_auth` (note: non-SRV, no DNS SRV lookup needed).
3. Run the server with an empty `MONGODB_URI` in scope so it can't shadow the file:
   `unset MONGODB_URI && ./mvnw spring-boot:run`
4. Verify: `curl -i -X POST -H 'Content-Type: application/json' -d '{"email":"x@y.com","password":"password123"}' http://localhost:8081/api/auth/signup` → expect `201`.

### Option B — Give me a working Atlas URI
1. You paste a fresh `mongodb+srv://USER:PASS@cluster....mongodb.net/...` (one whose DNS resolves from your machine).
2. I update `server/.env` to that URI.
3. You run `unset MONGODB_URI && ./mvnw spring-boot:run`.
4. Same `curl` verify.

### One diagnostic if you want to confirm the override before I touch anything
Run in your shell and paste the output:

```bash
echo "MONGODB_URI=[${MONGODB_URI}]"; grep -n MONGODB_URI ~/.bashrc ~/.profile ~/.zshrc 2>/dev/null; alias mvn 2>/dev/null
```

That will tell us exactly which file/alias is shadowing the `.env` value so we can remove it permanently instead of just working around it.

### Files I expect to touch (no code changes, only config)
- `server/.env` — replace the `MONGODB_URI` line with whichever URI you pick.

### Acceptance
- `./mvnw spring-boot:run` finishes with `Started ServerApplication` and **no** `IllegalArgumentException`.
- `GET http://localhost:8081/api/auth/config` → `200`.
- `POST http://localhost:8081/api/auth/signup` with a fresh email → `201` and a body containing the new user's `id`, `email`, and `"Account created successfully."`.

Which option do you want (A local Docker / B new Atlas URI / paste the diagnostic first), and shall I exit plan mode so I can run the restart and the `curl` verification myself?

<done>